export type Image = {
  albumId: number;
  id: number;
  title: string;
  path: string;
};

export type Photo = {
  albumId: number;
  id: number;
  title: string;
  url: string;
  thumbnailUrl: string;
};
